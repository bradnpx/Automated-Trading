{
  "name": "backtest",
  "private": true,
  "type": "module",
  "scripts": {
    "backtest": "pnpm --dir ../engine exec tsx ../backtest/src/cli.ts",
    "get-data": "pnpm --dir ../engine exec tsx src/cli/exportBacktestData.ts",
    "test": "pnpm --dir ../engine exec tsx ../backtest/src/test/run.ts",
    "check-types": "tsc --noEmit -p tsconfig.json",
    "lint": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@my-platform/types": "workspace:*"
  }
}
